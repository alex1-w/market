// import { createAsyncThunk } from "@reduxjs/toolkit";

// export const getUserInfo = createAsyncThunk('getUserInfo',
//     async (a, thunkApi) => {
//         try {
//             const res = await useGetUserInfoQuery()
//             return res
//         } catch (error) {
//             thunkApi.rejectWithValue({

//             })
//         }
//     });